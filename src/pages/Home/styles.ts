import styled from "styled-components";

export const Titulo = styled.div`
font-family: var(--font-oswald);
margin: 1rem 0;
display: flex;
align-items: center;

svg{
    fill: var(--color-rosa);
}
`;
